Unit SimulateInputScanner;

Interface

Uses Crt,
     DrillSimVariables,
     SimulateUserInput;

Procedure InputScan;

Implementation

Procedure InputScan;
Begin
  if keypressed then
  Begin
    Util:=ReadKey;
    writeln('Key Read');
    if Util = Slash then
    Begin
      CommandProcessor;        { Command - process it          }
    End else
    Begin
      Edited:=True;  { ...if it is then it's been edited }
      Input:=Util;
      InputCalc;              { Control - do recalculations based on input }
    End;
  End;
End;

Begin
End.